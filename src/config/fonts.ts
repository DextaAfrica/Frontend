/** Central font stacks. Replace these variables when brand fonts arrive from Figma. */
export const fontFamilies = {
  sans: "var(--font-sans)",
  serif: "var(--font-serif)",
  mono: "var(--font-mono)",
} as const;
